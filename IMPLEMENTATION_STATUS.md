# Implementation status — 2026-09-01

## Implemented

- Django 5.2 data model and initial migration for users, projects/memberships, upload sessions, image artifacts/builds/publications, template versions, immutable lab revisions, nodes/interfaces/links, deployments/device instances, durable operations, console/capture sessions, configurations, artifacts, and audit events.
- Database constraints for membership uniqueness, revision numbering, node/interface uniqueness, link endpoint distinction, idempotency keys, and one active target operation.
- Session-authenticated `/api/v1/` foundation with project scoping, viewer write denial, pagination, correlation IDs, OpenAPI, uploads, labs, deployments, and async operation acceptance.
- Resumable streamed uploads with exact offsets, SHA-256 verification, cancellation, size limits, quarantine paths, OCI/Docker archive distinction, traversal rejection, and unsupported-format outcomes.
- Safe bounded Containerlab YAML parser and export helper; interface reuse and unsafe fields are rejected.
- Clabernetes `c9s.run/v1alpha1` adapter pinned to 0.8.0 with explicit unsupported capability errors.
- React/TypeScript/React Flow workspace build with device nodes, interface handles, invalid peer reuse prevention, undo, pan/zoom/fit, minimap, validation and job panels.
- Multi-stage container build, non-root runtime, TLS gateway, fixed NodePort 30444, PostgreSQL, Redis, retained PVCs, worker, migration job, ServiceAccounts/RBAC, probes, and resource bounds.
- Actual cluster deployment and real two-node Clabernetes link traffic proof.
- Professional project, lab, image, topology-editor, runtime, console, diagnostics, configuration, import/export, and settings workflows outside Django admin.
- Session-bound browser consoles, reliable per-device restart, bounded ping diagnostics, authenticated PCAP capture/download, and bidirectional live link controls.
- Live link latency, jitter, loss, corruption, rate, disable, and restore controls, with bounded validation, bidirectional application, persisted conditions, idempotent operations, and audit events.
- Resumable 4 MiB image archive onboarding with pause/resume/cancel UX, optional expected checksum, server-side SHA-256, quarantine inspection, per-project deduplication, and audit events.
- Licensed Docker/OCI archive publication into single-node containerd with a checksum-derived immutable tag, isolated Kubernetes Job, project-scoped idempotent API, audit trail, build status, and image-library action.
- In-product project collaboration management with administrator/editor/viewer roles, delegated administrator controls, owner protection, cross-project isolation, exact-user lookup, and audited add/change/remove lifecycle.
- Enforced project quotas for labs, nodes per topology, active deployments, image storage/reservations, and members, with row-locked accounting, usage reporting, administrator controls, audit events, and a consistent conflict contract.
- Encrypted, versioned startup configuration delivery for supported appliance templates using deployment-scoped ConfigMaps and Clabernetes launcher mounts, plus content-free topology/configuration audit events.
- Production BGP reference lab using a resumably uploaded and locally published FRR 10.4.1 image, two configured routers, explicit eth1 link endpoints, established eBGP, learned loopback routes, and bidirectional routed reachability.
- Production nftables firewall reference lab using a dedicated checksum-published appliance, encrypted policy delivery, default-deny forwarding, permitted ICMP, denied TCP/8080, and named policy counters.
- Audited node-local image repair workflow with explicit operator intent, reconciling/failed states, and appliance-container readiness probes that prevent false-green deployments.
- Verified live configuration collection for FRR and nftables appliances with template-scoped commands, immutable encrypted versions, persistent deployment history, content-free audit records, and operator-only no-store downloads.
- Durable per-device Suspend and Resume controls that pause the nested appliance and isolate every linked data interface, with audited asynchronous operations and automatic reconciliation after launcher replacement.
- A hardened Celery Beat scheduler that reconciles active deployments every 30 seconds without a Kubernetes service-account token, writable root filesystem, or Linux capabilities.
- Durable one-time whole-lab start/stop scheduling from the runtime GUI with bounded future times, project-role authorization, optimistic cancellation, current-state eligibility rechecks, standard linked operation jobs, deterministic dispatch, and complete audit history.
- Dependency-aware staged startup for 2–20 stopped devices with GUI ordering, bounded 0–60 second spacing and five-minute total duration, optimistic preflight, conflict exclusion, one idempotent parent operation, resumable Celery countdown steps, per-device progress timestamps, and scheduled/completed audit records.
- Responsive live operational topology map using saved revision positions, real device/worker/launcher readiness, link endpoint and impairment state, contextual selection, authorized lifecycle/log/network-state actions, link-to-control navigation, and same-origin authenticated console launch.
- EVE-style Save As workflow in the topology workspace with an accessible modal, project authorization, quota/name-conflict handling, deep topology identity remapping, encrypted configuration re-versioning, and audit history.
- Topology revision-history workspace with immutable/deployed/draft status, counts and checksums, optimistic draft-conflict protection, idempotent restore into a new editable revision, encrypted configuration re-versioning, and audited provenance.
- Native self-service account and security page with profile/timezone management, normalized email, verified current-password changes, strengthened password validation, CSRF protection, session continuity, legacy-route safety, and content-free audit events.
- Explicit GUI-only operator contract: topology Backup/Restore is labeled as a product-native bundle workflow, while Containerlab/Kubernetes YAML remains internal to the validated runtime adapter.
- Bounded per-device appliance and Clabernetes launcher log inspection through audited worker jobs, with operator authorization, selectable 20-1000 line limits, 100 KB output caps, no-store polling, refresh, and copy controls.
- Server-validated lab backup restore preview with bundle checksum, topology/configuration/template/image inventory, deployability issues, explicit impact confirmation, stale-draft protection, idempotent replay, immutable/running revision preservation, and audit events.
- Audited configuration-version comparison and safe restore with same-device validation, bounded no-store unified diffs, explicit impact preview, stale-draft protection, idempotent replay, newly encrypted draft configuration versions, and immutable/running revision preservation.
- GUI-native appliance traceroute with ready-device authorization, literal IP validation, bounded hop/probe/timeout controls, idempotent audited worker execution, and capped output from the selected nested appliance.
- Guarded image artifact deletion with server-counted publication/build/revision/job references, explicit impact preview, optimistic checksum confirmation, idempotent audited soft deletion, bounded owned-file removal, quota release, hidden deleted records, and checksum reuse.
- Native lab metadata editing and guarded lab deletion with immutable project ownership, active-runtime/job refusal, optimistic concurrency, idempotent audited soft deletion, quota release, hidden deleted workspaces, preserved revision/deployment history, and safe name reuse.
- Administrator-only project metadata management and guarded retirement with active lab/image/upload/runtime/job blockers, stale-preview protection, idempotent audit history, retired-project write rejection, complete selector/API hiding, preserved records, and safe owner/name reuse.
- A 45-screen, read-only Firefox training capture catalog covering project and lab lifecycle, visual topology design and arrangement, verified backup/restore, image onboarding/deletion, deployment/device lifecycle, selected-node actions, guarded reset/removal, safe redeploy preview, runtime logs and network state, link controls, ping/traceroute diagnostics, packet capture, console, configuration history/compare/restore, templates, jobs, security, and API discovery.
- Reproducible frontend supply chain with a frozen pnpm lockfile, explicitly approved native build dependency, patched Vite/Vitest/esbuild toolchain, zero known dependency advisories, executable topology utility tests, and no externally hosted font dependency at runtime.
- Platform-administrator device-template management with structured no-YAML creation, immutable activation history, optimistic and idempotent version changes, bounded resources and interfaces, reviewed FRR/nftables configuration presets, read-only operator access, preserved lab-version pins, and audited metadata-only provenance.
- Native topology notes and colored regions with canvas placement, move/resize, structured styling and layering, inspector editing, undo/redo, autosave, checksum participation, backup/restore, export, and deep-clone preservation without YAML.
- Per-device CPU and memory governance from immutable template versions through Clabernetes resource policy to matching Kubernetes launcher requests/limits, with the pinned allocation visible in the runtime GUI.
- EVE-style multi-device canvas selection with grouped movement, collision-free subgraph duplication, internal-link remapping, bulk deletion, undo/redo, keyboard controls, and preservation of template/image/configuration pins.
- Deterministic topology arrangement for linked components with note/region obstacle avoidance, undoable row/column alignment for multi-selected devices, and live device-palette filtering by template name, Containerlab kind, or category.
- Guarded per-device reset to the immutable deployed revision with stale-preview protection, active-capture exclusion, console revocation, idempotent audited execution, pinned startup-configuration restoration, and peer-preserving launcher replacement.
- Server-preflighted selected-device start, stop, restart, suspend, and resume workflows with atomic eligibility checks, all-or-none scheduling, deterministic idempotency replay, independent job visibility, aggregate and per-device audit events, and responsive runtime selection controls.
- Guarded native administrator password recovery with active browser/console impact preview, policy validation, immediate session revocation, mandatory next-login rotation, corrected post-login routing, and content-free audit provenance.
- Structured live appliance inspection for interfaces, IPv4/IPv6 addresses, routes, and neighbors using fixed bounded commands, normalized result limits, asynchronous project authorization, idempotency, content-free auditing, and responsive GUI tables.
- Guarded EVE-style selected-device reset with all-or-none capture/readiness preflight, per-device saved-baseline and console impact, optimistic concurrency, independent idempotent jobs, console revocation, launcher replacement, and immutable configuration restoration.
- Read-only topology revision comparison with stable device and canonical link matching, structured added/removed/modified summaries, canvas/object change reporting, image/template/interface/configuration-checksum awareness, project authorization, and content-free audit evidence.
- Project-scoped hierarchical lab folders with nested paths, native create/edit/assignment controls, cross-project and cycle protection, an eight-level depth bound, non-cascading guarded deletion, and content-free audit history.
- EVE-style folder navigation with direct-child catalog scoping, linked breadcrumbs, contextual project/parent preselection, permission-aware creation controls, invalid/foreign-folder isolation, and folder-preserving post-action redirects.
- Native security audit trail with project/platform scoping, actor/action/target/correlation/time filters, bounded escaped metadata evidence, 50-row pagination, administrator-authorized 5,000-row CSV export, spreadsheet-injection protection, no-store headers, and self-auditing export provenance.

## Acceptance results

| # | Check | Result | Evidence / exact blocker |
|---|---|---|---|
| 1 | GUI through 30444 | PASS | TLS admin login returned HTTP 200 at `192.168.1.148:30444`. |
| 2 | Authentication/project authorization | PASS | Django admin login deployed; API guessed-UUID and viewer-write tests pass. |
| 3 | Supported upload/preparation | PASS | A real 8,585,216-byte Alpine Docker archive was resumed across three chunks, checksum-matched, safely identified, validated, audited, and deduplicated; malformed input remained unsupported. |
| 4 | Invalid formats stay undeployable | PASS | Unit tests verify malformed data and traversal archives are unsupported. |
| 5 | Published images pull through runtime layers | PASS | Uploaded Alpine archive published through the API; Clabernetes copied its immutable node-local tag into the launcher Docker daemon. |
| 6 | Layout persists | PASS | Editor draft save/load persists node positions, annotations, interfaces, links, images, and encrypted startup configurations. |
| 7 | Interface links validated | PASS | Parser and serializer reject duplicate point-to-point interfaces. |
| 8 | Real Kubernetes lab workload | PASS | `studio-smoke` TopologyReady with two launcher/device containers and VXLAN link. |
| 9 | Duplicate deploy idempotency | PASS | DB constraints and unit test cover idempotency; cluster replay test not run. |
| 10 | Device vs pod readiness | PASS | Separate observed readiness fields; Clabernetes node/topology readiness inspected. |
| 11 | Browser console reaches device | PASS | Session-bound, expiring, project-scoped WebSocket console was exercised against live Alpine devices; viewers are read-only. |
| 12 | Reference-lab traffic | PASS | Two-AS BGP and default-deny nftables firewall reference acceptance both pass with positive and negative traffic evidence. |
| 13 | PCAP download | PASS | Live eth1 capture downloaded through the authenticated API; checksum matched and tcpdump decoded 14 genuine ICMP/ARP packets with internal stop frames removed. |
| 14 | Stop preserves saved lab/config | PASS | Runtime stop/redeploy preserves immutable revision data, saved topology, and encrypted startup configuration records. |
| 15 | Redeploy pinned revisions/images | PASS | Adapter accepts registry digests or checksum-derived node-local publications; a fresh saved lab deployed the published Alpine 3.22.5 image and reached ready. |
| 16 | Restart avoids duplicate labs | PASS | Live per-device restart replaced only the selected launcher pod; multi-stage reconciliation restored readiness without creating another topology. |
| 17 | Cross-project isolation | PASS | Guessed UUID API test returns 404. |
| 18 | Cleanup preserves unrelated resources | PASS | Only `containerlab` and owned PVs touched; `trading` namespace unchanged. |
| 19 | Existing workloads unaffected | PASS | Trading workloads remained running during inspection/deployment. |
| 20 | Backup/restore | PASS | A 1,888-byte GUI-native backup was server-previewed and restored into an independent lab with two devices, one link, one configuration, and unchanged source state; a deployable BGP backup then replaced that draft under concurrency protection. |
| 21 | Project collaboration lifecycle | PASS | Live owner assigned admin/editor/viewer roles; editor mutation returned 403; delegated admin add/change/remove returned 201/200/204; owner and viewer pages rendered the correct controls. |
| 22 | Project resource governance | PASS | Live one-unit limits allowed the first lab and rejected excess labs, members, image reservations, and topology nodes with typed 409 conflicts; usage/UI/audit checks passed. |
| 23 | Versioned startup configuration | PASS | FRR configs remained encrypted in PostgreSQL, materialized into deployment-scoped ConfigMaps, mounted into launchers, and applied to both ready devices. |
| 24 | BGP reference lab | PASS | FRR neighbor reached Established with one received prefix; 10.2.2.2/32 installed via BGP/eth1; both sourced loopback pings passed 3/3 with 0% loss. |
| 25 | Firewall reference lab | PASS | Routed ICMP passed 3/3 with 0% loss; a locally verified TCP/8080 listener was unreachable through the firewall; nftables recorded 1 allowed ICMP flow and 3 denied TCP SYNs. |
| 26 | Node-local image repair | PASS | A missing FRR node image was republished through the authenticated audited product operation, restored to containerd, and enabled waiting launchers without manual runtime import. |
| 27 | Appliance readiness | PASS | Reconciliation probes the nested appliance container and keeps the deployment in `deploying` when Clabernetes reports a ready Node without a running device. |
| 28 | Live configuration collection | PASS | FRR and firewall collection operations succeeded; repeated firewall collection created v2; three versions remained encrypted at rest; downloaded payload checksums matched and responses used `no-store`/`nosniff`. |
| 29 | Per-device suspend/resume | PASS | Suspending the live server paused its appliance, set `server-eth1` down, and produced 100% packet loss; Resume unpaused it, restored the link, and recovered 3/3 packets with 0% loss. |
| 30 | Convergent device lifecycle | PASS | After the suspended server launcher was deleted, Kubernetes assigned a new pod and UID; periodic reconciliation preserved suspend intent, re-paused the replacement, kept its data interface down, and maintained 100% packet loss until Resume. |
| 31 | Lab Save As / deep clone | PASS | A deployed immutable two-router BGP revision was copied through the production API with new revision/node/configuration identities, preserved two nodes/one link/two encrypted FRR configs, a non-null editable draft, and an audit event; the cloned lab deployed, established eBGP, and passed 3/3 sourced loopback pings in both directions. |
| 32 | Revision history and restore | PASS | The deployed BGP revision remained immutable and its original runtime stayed healthy while restore created revision 2 as a new editable draft with two encrypted FRR configs; replay returned the same result, a stale draft token returned typed 409, and deploying the restored revision established eBGP and passed 3/3 routed pings both ways. |
| 33 | Self-service account security | PASS | A temporary production account rendered the native page, rejected an incorrect current password, normalized and saved profile/timezone data, changed to a policy-compliant password, authenticated with the replacement credential, retained its active session, and emitted content-free profile/password audit events; the temporary user was then removed. |
| 34 | Packet-corruption impairment | PASS | The authenticated product API applied 0.5% corruption to a live two-router link, `tc netem` reported `corrupt 0.5%` on both launcher endpoints, and a second idempotent operation restored the persisted/runtime condition to 0%. |
| 35 | Safe whole-lab redeploy | PASS | Firefox verified the read-only impact preview; an authenticated audited redeploy recreated a stopped two-device/one-link Alpine runtime from pinned revision 1, reached 2/2 ready and `running`, and a final stop restored its original stopped state. |
| 36 | Device and launcher logs | PASS | Authenticated worker jobs collected 2,682 bytes of live FRR appliance startup output and 11,799 bytes of Clabernetes launcher output for the same router; both completed successfully through bounded, audited, no-store GUI contracts. |
| 37 | Restored topology runtime | PASS | The independently restored two-router BGP backup reached 2/2 ready after audited node-local image repair; product diagnostics passed 3/3 with 0% loss in both directions, the test runtime was stopped, and the displaced capacity runtime was returned to running. |
| 38 | Configuration compare and safe restore | PASS | Firewall collected versions 1 and 2 were compared through the authenticated no-store API; version 2 then produced editable revision 2 under idempotency and draft-concurrency protection while the original immutable deployment remained running with 3/3 devices ready and an unchanged revision. |
| 39 | Appliance traceroute | PASS | The authenticated product operation executed bounded traceroute inside live FRR router r1 toward 10.2.2.2 and returned the real one-hop path (`10.2.2.2`, 0.018 ms); the same completed workflow was captured in Firefox with explicit probe, timeout, and hop bounds. |
| 40 | Guarded image deletion | PASS | A published firewall artifact was correctly protected by 1 publication, 1 build, and 2 lab revisions; a disposable 66-byte malformed upload showed zero references, was previewed in Firefox, removed its owned PVC file, released exactly 66 quota bytes, disappeared from the API/UI, returned the same idempotent replay result, and retained audit provenance. |
| 41 | Guarded lab lifecycle | PASS | A running production lab was protected by its active deployment; a disposable lab was created and edited through the authenticated product API, previewed with zero blockers, soft-deleted under optimistic and idempotency controls, hidden from API and workspace with history retained, returned quota to its baseline, and allowed the same name to be reused. Firefox captured the native confirmation workflow. |
| 42 | Guarded project lifecycle | PASS | The populated production project was protected by three active labs; an empty disposable project was created and edited, retired under optimistic/idempotent controls, hidden from API and portal, rejected as a destination for new labs, replayed with the same result, and recreated with the same owner/name. Firefox captured the native administrator confirmation workflow. |
| 43 | Guarded runtime removal | PASS | A disposable two-device/one-link runtime was deployed to ready state, previewed in Firefox, and removed through the authenticated product workflow. Its owned namespace disappeared independently from Kubernetes, both device compute records were cleared, refresh/redeploy were rejected after the terminal transition, idempotent replay returned the original job, and immutable lab revision/history remained available. The displaced quota specimen was restored to running state. |
| 44 | Frontend supply-chain and isolated-browser readiness | PASS | A clean frozen install resolved the audited dependency graph with zero known vulnerabilities; Vitest 4.1.11 executed two topology interface tests, Vite 8.2.2 produced the production bundle, and the exact image was built and deployed. Firefox then authenticated and rendered the portal plus React Flow workspace with zero console errors, zero failed requests, and no Google Fonts/network dependency. |
| 45 | Versioned device-template lifecycle | PASS | The production API created GUI Template Acceptance v1, a disposable lab saved a node pinned to v1, and activation of v2 expanded the live palette from two to four interfaces while that saved node remained pinned to v1. PostgreSQL acceptance exposed and then verified the fix for nullable-join row locking; history returned v2/v1, Firefox captured the full management screen, audit and operation records were retained, and the disposable lab was removed through guarded deletion. |
| 46 | Topology canvas objects | PASS | Firefox created and reloaded a styled security region plus an operational note using only canvas and inspector controls. Both objects retained position, dimensions, color, text, font, and layer through autosave; the native export contained both objects, a production deep clone preserved them exactly, and the disposable clone was removed through guarded deletion. |
| 47 | Enforced device compute resources | PASS | GUI Template Acceptance v2 pinned 250m CPU and 256Mi memory to a disposable device. Its exact Clabernetes Topology resource map and live launcher-container requests/limits matched, the appliance reached Ready, and Firefox displayed the allocation with no failed requests or console errors. Visual QA also exposed and verified a deterministic image-build fix that clears stale collected assets. |
| 48 | Multi-device subgraph duplication | PASS | Firefox selected a linked two-router BGP subgraph, duplicated it, saved, reloaded, and rendered four uniquely named devices with two independent links. API verification proved unique UUIDs, identical source/copy configuration checksums, preserved template/image pins, and exclusion of external links. The exercise exposed and verified deterministic upgrade of pre-canvas annotations without mutating immutable source revisions. |
| 49 | Topology arrangement and discovery | PASS | Firefox arranged four devices into deterministic connected groups, verified no device intersected the saved canvas note, aligned two selected devices into a row, exercised matching and empty-state device filtering, saved the draft, reloaded it, and confirmed the layout persisted. Arrange and alignment remain undoable and require no YAML. |
| 50 | Guarded per-device reset | PASS | Firefox previewed and reset BGP router r1 from immutable revision 2. Its launcher UID changed from `6f141a42-9cc7-4ba8-b9bc-a45cf23c30a1` to `68f374a0-c4b2-4433-a7db-c26f66cfd1fe`, peer r2 retained UID `db73b442-953d-4147-abf7-e4eeda4a344a`, both returned ready, the result recorded pinned configuration restoration, and a post-reset product ping passed 3/3 with 0% loss. |
| 51 | Selected-device lifecycle | PASS | Firefox selected live BGP routers r1 and r2, received an all-eligible suspend preview, scheduled two independent suspend jobs, verified both devices suspended without changing either launcher UID, then preflighted and scheduled two resume jobs. Both returned ready with their original launcher identities and routed reachability recovered to 3/3 packets with 0% loss after bounded protocol convergence. |
| 52 | Safe concurrent topology editing | PASS | Two isolated Firefox sessions opened the same production BGP topology as different project editors. Admin acquired a renewable token-bound lease; the second editor received the named read-only banner with expiry, disabled Save and canvas mutations, and tokenless mutation protection returned 409. PostgreSQL acceptance exposed and verified removal of an invalid nullable outer join from row-locking queries. |
| 53 | Native user administration | PASS | Firefox created a policy-compliant local operator in the staff-only Studio directory, assigned a viewer project membership through the native access panel, previewed and confirmed guarded deactivation, verified disabled sign-in was rejected, reactivated the account, and verified sign-in succeeded with its membership preserved. Account creation, deactivation, and activation audit events were confirmed before cleanup; Settings contains no Django-admin link. |
| 54 | Guarded credential recovery | PASS | Firefox established an operator session, showed it in the administrator reset preview, confirmed a temporary password, and verified the existing browser was forced back to sign-in. The reset credential then authenticated only into mandatory Account & security rotation; after replacement, normal dashboard access resumed. Password-reset and forced-change events contain no credential material. |
| 55 | Live device network-state inspection | PASS | Firefox opened the inspector for production FRR router r1 and rendered 3 interfaces, 23 routes, and 1 neighbor from fixed bounded appliance queries. The result included `eth1` at 10.0.12.1/30, the BGP-learned 10.2.2.2 route through 10.0.12.2, and its data-plane neighbor. The exact `3a504e2` image ran across all four Studio services with no console errors or failed application requests. |
| 56 | Guarded selected-device reset | PASS | Firefox selected production BGP routers r1 and r2, reviewed the all-eligible reset preview, and scheduled two independently audited reset jobs. Both restored their saved revision-2 configurations and replaced launcher UIDs (`d38cedd6…` → `4ec6a2f6…`, `bb7f9b13…` → `28bb4703…`); both returned ready and a GUI diagnostic recovered 3/3 routed packets with 0% loss. The exact `4f7f359` image ran across all four Studio services. |
| 57 | Topology revision comparison | PASS | Firefox selected Alpine Connectivity QA revisions 1 and 2 and rendered one modified device with only its startup-configuration checksum changed; devices added/removed, links, canvas, and objects correctly remained zero/unchanged. Audit metadata contained only revision numbers and structural counts, both revision checksums remained unchanged, and exact release `c1e161a` ran across all four Studio services. |
| 58 | Hierarchical lab folders | PASS | Firefox created `Training Evidence 48 / BGP Operations`, assigned Alpine Connectivity QA to the child, displayed the full path and live counts, and blocked deletion with exactly one dependent lab. The lab was restored to root and both disposable folders were deleted through the GUI; no console errors or failed requests occurred, and exact release `b35a65c` ran across all four Studio services. |
| 59 | Navigable lab folder browser | PASS | Firefox navigated from the library root into `Navigable Evidence 49 / Routing Validation`, verified contextual subfolder preselection and linked breadcrumbs, moved Alpine Connectivity QA into the child, and rendered exactly that one direct lab. The edit returned to the same folder, no console/request errors occurred, cleanup restored the lab and removed both folders, and exact release `542c6eb` ran across all four Studio services. |
| 60 | Native security audit trail | PASS | Firefox filtered Production Runtime QA to eight real `lab_folder.*` events, expanded bounded deletion metadata, and downloaded the authorized result as a 1,804-byte, nine-line CSV with the expected eight-column schema. The export emitted a content-free `audit.exported` record with its row count and filter state, no console/request errors occurred, and exact release `9e5706e` ran across all four Studio services. |
| 61 | Scheduled lab lifecycle | PASS | Firefox created and cancelled a future stop, scheduled a real start, observed Celery Beat dispatch it to linked operation `ece00cc8-4af7-4f53-adc9-6c9ddfa9cb90`, and verified the job succeeded and the runtime reached running. The GUI then restored the acceptance lab to stopped; PostgreSQL reported two dispatched, two cancelled, and zero pending schedules, while exact release `1f7931a` ran across all four Studio services. |
| 62 | Dependency-aware staged device start | PASS | Firefox stopped both production BGP routers, reordered the guarded preview to `r2 → r1`, and submitted an eight-second sequence. PostgreSQL recorded starts 8.213 seconds apart under one 100%-complete operation, both replacement launchers returned ready with no desired-state residue, and a product ping passed 3/3 with 0% loss. Live acceptance exposed and verified removal of an invalid nullable outer join from the step lock; exact release `d6c50f8` ran across all four Studio services with zero active sequences. |
| 63 | Live operational topology | PASS | Firefox rendered production BGP revision 2 with `r1` at (260,300), `r2` at (660,300), and one healthy `r1:eth1 ↔ r2:eth1` link. Node selection displayed live worker `trading`, launcher, and eight interfaces; link selection navigated to controls; map-to-console reached `connected`. Acceptance exposed and fixed transparent-layer pointer interception. Both routers remained ready, link conditions remained empty, and exact release `7e05c7d` ran across all four services. |

Automated tests: **143 backend and 7 frontend passed**. Django checks and migration drift checks: **pass**. React TypeScript/Vite production build: **pass**. Frontend dependency audit: **zero known vulnerabilities**. Firefox training catalog: **53 live screens verified**. Helm lint/render: **pass**. Native runtime ping: **3 transmitted, 3 received, 0% loss, 0.445 ms average RTT**. Live appliance traceroute: **1 verified hop to 10.2.2.2**. Live network-state inspection: **3 interfaces, 23 routes, and 1 neighbor**. Post-staged-start BGP reachability: **3 transmitted, 3 received, 0% loss, 0.264 ms average RTT**. Post-selected-reset BGP reachability: **3 transmitted, 3 received, 0% loss, 0.865 ms average RTT**. Post-reset BGP reachability: **3 transmitted, 3 received, 0% loss, 0.297 ms average RTT**. Post-bulk-resume BGP reachability: **3 transmitted, 3 received, 0% loss, 0.839 ms average RTT**. Bidirectional 120 ms link condition: **240.563 ms average RTT**. Disabled link: **100% loss**. Restored qdiscs: **native `noqueue` on both endpoints**.

## Known limitations

- No `/dev/kvm`; all VM-backed vendor templates are unsupported on the current worker.
- No dynamic StorageClass; this installation uses three application-owned static hostPath PVs on the single node and is not highly available.
- Node-local publication is intentionally a single-node mode. Multi-node and highly available installations still require a trusted OCI registry so every worker can resolve the same immutable image.
- Clabernetes 0.8.0 emitted an auxiliary Alpine puller warning (`exit` executable missing), although launcher pulls, topology readiness, device creation, and traffic all succeeded.
- Shared Containerlab `bridge` network objects are not exposed on this installation: the backend requires a pre-existing Linux bridge on the Kubernetes worker, while Clabernetes is intentionally namespace-isolated. Creating host networking from the application would break that isolation boundary; point-to-point topology links remain fully supported in the GUI.
