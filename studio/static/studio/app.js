document.addEventListener('DOMContentLoaded',()=>{const menu=document.getElementById('menu-toggle');const sidebar=document.getElementById('sidebar');menu?.addEventListener('click',()=>sidebar?.classList.toggle('open'));document.addEventListener('click',e=>{if(window.innerWidth<=760&&sidebar?.classList.contains('open')&&!sidebar.contains(e.target)&&e.target!==menu)sidebar.classList.remove('open')});document.querySelectorAll('.password-toggle').forEach(button=>button.addEventListener('click',()=>{const input=button.parentElement.querySelector('input');const show=input.type==='password';input.type=show?'text':'password';button.textContent=show?'Hide':'Show'}));const search=document.getElementById('catalog-search');search?.addEventListener('input',()=>{let shown=0;document.querySelectorAll('.catalog-row').forEach(row=>{const match=row.dataset.search.includes(search.value.trim().toLowerCase());row.hidden=!match;if(match)shown++});const empty=document.getElementById('no-results');if(empty)empty.style.display=shown?'none':'flex'});const path=location.pathname;document.querySelectorAll('.nav-item').forEach(link=>{link.classList.remove('active');if(link.getAttribute('href')===path||(path!=='/'&&path.startsWith(link.getAttribute('href'))))link.classList.add('active')})});
